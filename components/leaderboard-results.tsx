import Link from "next/link";
import type { PlayerMetricDefinition } from "@/lib/player-performance";
import type { LeaderboardRow } from "@/lib/leaderboards";
const testDate = (date: string) => new Date(`${date}T00:00:00Z`).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric", timeZone: "UTC" });
export function LeaderboardResults({ rows, metric, unit }: { rows: LeaderboardRow[]; metric: PlayerMetricDefinition; unit: string }) {
  if (!rows.length) return <section className="panel p-8 text-center"><h2 className="mb-2 text-lg font-bold">No Reviewed Results Yet</h2><p className="muted mb-0">Choose another measurement or testing period. Results appear after a staff member reviews and saves matching measurements.</p></section>;
  const neutral = metric.direction === "neutral";
  return <section className="panel overflow-hidden"><div className="border-b border-gray-200 p-5 sm:p-6"><h2 className="mb-1 text-lg font-bold">{metric.label} <span className="muted text-sm font-normal">· {rows.length} measured athletes</span></h2><p className="muted mb-0 text-sm">{neutral ? "Listed from highest to lowest value. Body and spin values are numerical comparisons, not a health or performance rating." : `Latest comparable reading per athlete · ${metric.direction === "lower" ? "Lowest" : "Highest"} values first · Equal values share a place.`}</p></div>
    <div className="table-wrap"><table><caption className="sr-only">{metric.label} team results in {unit}</caption><thead><tr>{!neutral && <th>Place</th>}<th>Athlete</th><th>Result</th><th className="hidden sm:table-cell">Test Date</th></tr></thead><tbody>{rows.map(row => <tr key={row.athleteCode}>{!neutral && <td className="font-semibold">{row.rank}</td>}<td><span className="block break-words font-semibold">{row.profileId ? <Link href={`/athletes/${row.profileId}`}>{row.name}</Link> : row.name}</span><span className="muted text-xs">{row.athleteCode}{row.jerseyNumber !== null ? ` · #${row.jerseyNumber}` : ""}{row.position ? ` · ${row.position}` : ""}</span><time dateTime={row.measuredAt} className="muted mt-1 block text-xs sm:hidden">{testDate(row.measuredAt)}</time></td><td className="font-semibold">{row.derived ? <span title={`Exact calculated value: ${String(row.value)} ${unit}`}>≈{row.value.toLocaleString("en-US", { maximumFractionDigits: 1 })}</span> : <span className="break-all">{String(row.value)}</span>} {unit}{row.derived && <span className="muted block text-xs font-normal">Calculated from same-report muscle and weight</span>}</td><td className="hidden sm:table-cell"><time dateTime={row.measuredAt}>{testDate(row.measuredAt)}</time></td></tr>)}</tbody></table></div>
  </section>;
}
